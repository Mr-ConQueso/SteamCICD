services:
  steamcicd:
    image: ghcr.io/mr-conqueso/steamcicd:latest
    container_name: steamcicd
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - ./downloads:/app/downloads
      - ./steamworks_sdk:/app/steamworks_sdk
    restart: unless-stopped